<%= form_for(@pdf_record) do |f| %>
  <% if @pdf_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pdf_record.errors.count, "error") %> prohibited this pdf_record from being saved:</h2>

      <ul>
      <% @pdf_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :urls %><br />
    <%= f.text_area :urls %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
